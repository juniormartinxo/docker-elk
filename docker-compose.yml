services:
  # The 'setup' service runs a one-off script which initializes users inside
  # Elasticsearch — such as 'logstash_internal' and 'kibana_system' — with the
  # values of the passwords defined in the '.env' file. It also creates the
  # roles required by some of these users.
  #
  # This task only needs to be performed once, during the *initial* startup of
  # the stack. Any subsequent run will reset the passwords of existing users to
  # the values defined inside the '.env' file, and the built-in roles to their
  # default permissions.
  #
  # By default, it is excluded from the services started by 'docker compose up'
  # due to the non-default profile it belongs to. To run it, either provide the
  # '--profile=setup' CLI flag to Compose commands, or "up" the service by name
  # such as 'docker compose up setup'.
  setup:
    profiles:
      - setup
    build:
      context: setup/
      args:
        ELASTIC_VERSION: ${ELASTIC_VERSION}
    container_name: ${CONTAINER_SETUP_NAME}
    init: true
    volumes:
      - ./setup/entrypoint.sh:/entrypoint.sh:ro,Z
      - ./setup/lib.sh:/lib.sh:ro,Z
      - ./setup/roles:/roles:ro,Z
    environment:
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD:-}
      LOGSTASH_INTERNAL_PASSWORD: ${LOGSTASH_INTERNAL_PASSWORD:-}
      KIBANA_SYSTEM_PASSWORD: ${KIBANA_SYSTEM_PASSWORD:-}
      METRICBEAT_INTERNAL_PASSWORD: ${METRICBEAT_INTERNAL_PASSWORD:-}
      FILEBEAT_INTERNAL_PASSWORD: ${FILEBEAT_INTERNAL_PASSWORD:-}
      HEARTBEAT_INTERNAL_PASSWORD: ${HEARTBEAT_INTERNAL_PASSWORD:-}
      MONITORING_INTERNAL_PASSWORD: ${MONITORING_INTERNAL_PASSWORD:-}
      BEATS_SYSTEM_PASSWORD: ${BEATS_SYSTEM_PASSWORD:-}
      ELASTIC_PORT: ${ELASTIC_PORT}
      NETWORK_NAME: ${NETWORK_NAME}
      CONTAINER_SETUP_NAME: ${CONTAINER_SETUP_NAME}
      CONTAINER_ELASTICSEARCH_NAME: ${CONTAINER_ELASTICSEARCH_NAME}
      CONTAINER_KIBANA_NAME: ${CONTAINER_KIBANA_NAME}
      KIBANA_PORT: ${KIBANA_PORT}
      CONTAINER_LOGSTASH_NAME: ${CONTAINER_LOGSTASH_NAME}
      LOGSTASH_PORT: ${LOGSTASH_PORT}
      CONTAINER_EXTENSION_CURATOR_NAME: ${CONTAINER_EXTENSION_CURATOR_NAME}
      CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME: ${CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME}
      EXTENSION_ENTERPRISE_SEARCH_PORT: ${EXTENSION_ENTERPRISE_SEARCH_PORT}
      CONTAINER_EXTENSION_FILEBEAT_NAME: ${CONTAINER_EXTENSION_FILEBEAT_NAME}
      CONTAINER_EXTENSION_FLEET_NAME: ${CONTAINER_EXTENSION_FLEET_NAME}
      EXTENSION_FLEET_PORT: ${EXTENSION_FLEET_PORT}
      APM_SERVER_PORT: ${APM_SERVER_PORT}
      CONTAINER_EXTENSION_HEARTBEAT_NAME: ${CONTAINER_EXTENSION_HEARTBEAT_NAME}
      CONTAINER_EXTENSION_METRICBEAT_NAME: ${CONTAINER_EXTENSION_METRICBEAT_NAME}
    networks:
      - ${NETWORK_NAME}
    depends_on:
      - elasticsearch

  elasticsearch:
    build:
      context: elasticsearch/
      args:
        ELASTIC_VERSION: ${ELASTIC_VERSION}
    container_name: ${CONTAINER_ELASTICSEARCH_NAME}
    volumes:
      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro,Z
      - elasticsearch:/usr/share/elasticsearch/data:Z
    ports:
      - 9200:9200
      - 9300:9300
    environment:
      node.name: elasticsearch
      ES_JAVA_OPTS: -Xms512m -Xmx512m
      LOGSTASH_INTERNAL_PASSWORD: ${LOGSTASH_INTERNAL_PASSWORD:-}
      KIBANA_SYSTEM_PASSWORD: ${KIBANA_SYSTEM_PASSWORD:-}
      METRICBEAT_INTERNAL_PASSWORD: ${METRICBEAT_INTERNAL_PASSWORD:-}
      FILEBEAT_INTERNAL_PASSWORD: ${FILEBEAT_INTERNAL_PASSWORD:-}
      HEARTBEAT_INTERNAL_PASSWORD: ${HEARTBEAT_INTERNAL_PASSWORD:-}
      MONITORING_INTERNAL_PASSWORD: ${MONITORING_INTERNAL_PASSWORD:-}
      BEATS_SYSTEM_PASSWORD: ${BEATS_SYSTEM_PASSWORD:-}
      ELASTIC_PORT: ${ELASTIC_PORT}
      NETWORK_NAME: ${NETWORK_NAME}
      CONTAINER_SETUP_NAME: ${CONTAINER_SETUP_NAME}
      CONTAINER_ELASTICSEARCH_NAME: ${CONTAINER_ELASTICSEARCH_NAME}
      CONTAINER_KIBANA_NAME: ${CONTAINER_KIBANA_NAME}
      KIBANA_PORT: ${KIBANA_PORT}
      CONTAINER_LOGSTASH_NAME: ${CONTAINER_LOGSTASH_NAME}
      LOGSTASH_PORT: ${LOGSTASH_PORT}
      CONTAINER_EXTENSION_CURATOR_NAME: ${CONTAINER_EXTENSION_CURATOR_NAME}
      CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME: ${CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME}
      EXTENSION_ENTERPRISE_SEARCH_PORT: ${EXTENSION_ENTERPRISE_SEARCH_PORT}
      CONTAINER_EXTENSION_FILEBEAT_NAME: ${CONTAINER_EXTENSION_FILEBEAT_NAME}
      CONTAINER_EXTENSION_FLEET_NAME: ${CONTAINER_EXTENSION_FLEET_NAME}
      EXTENSION_FLEET_PORT: ${EXTENSION_FLEET_PORT}
      APM_SERVER_PORT: ${APM_SERVER_PORT}
      CONTAINER_EXTENSION_HEARTBEAT_NAME: ${CONTAINER_EXTENSION_HEARTBEAT_NAME}
      CONTAINER_EXTENSION_METRICBEAT_NAME: ${CONTAINER_EXTENSION_METRICBEAT_NAME}
      # Bootstrap password.
      # Used to initialize the keystore during the initial startup of
      # Elasticsearch. Ignored on subsequent runs.
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD:-}
      # Use single node discovery in order to disable production mode and avoid bootstrap checks.
      # see: https://www.elastic.co/guide/en/elasticsearch/reference/current/bootstrap-checks.html
      discovery.type: single-node
    networks:
      - ${NETWORK_NAME}
    restart: unless-stopped

  logstash:
    build:
      context: logstash/
      args:
        ELASTIC_VERSION: ${ELASTIC_VERSION}
    container_name: ${CONTAINER_LOGSTASH_NAME}
    volumes:
      - ./logstash/config/logstash.yml:/usr/share/logstash/config/logstash.yml:ro,Z
      - ./logstash/pipeline:/usr/share/logstash/pipeline:ro,Z
    ports:
      - 5044:5044
      - 50000:50000/tcp
      - 50000:50000/udp
      - 9600:9600
    environment:
      LS_JAVA_OPTS: -Xms256m -Xmx256m
      LOGSTASH_INTERNAL_PASSWORD: ${LOGSTASH_INTERNAL_PASSWORD:-}
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD:-}
      KIBANA_SYSTEM_PASSWORD: ${KIBANA_SYSTEM_PASSWORD:-}
      METRICBEAT_INTERNAL_PASSWORD: ${METRICBEAT_INTERNAL_PASSWORD:-}
      FILEBEAT_INTERNAL_PASSWORD: ${FILEBEAT_INTERNAL_PASSWORD:-}
      HEARTBEAT_INTERNAL_PASSWORD: ${HEARTBEAT_INTERNAL_PASSWORD:-}
      MONITORING_INTERNAL_PASSWORD: ${MONITORING_INTERNAL_PASSWORD:-}
      BEATS_SYSTEM_PASSWORD: ${BEATS_SYSTEM_PASSWORD:-}
      ELASTIC_PORT: ${ELASTIC_PORT}
      NETWORK_NAME: ${NETWORK_NAME}
      CONTAINER_SETUP_NAME: ${CONTAINER_SETUP_NAME}
      CONTAINER_ELASTICSEARCH_NAME: ${CONTAINER_ELASTICSEARCH_NAME}
      CONTAINER_KIBANA_NAME: ${CONTAINER_KIBANA_NAME}
      KIBANA_PORT: ${KIBANA_PORT}
      CONTAINER_LOGSTASH_NAME: ${CONTAINER_LOGSTASH_NAME}
      LOGSTASH_PORT: ${LOGSTASH_PORT}
      CONTAINER_EXTENSION_CURATOR_NAME: ${CONTAINER_EXTENSION_CURATOR_NAME}
      CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME: ${CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME}
      EXTENSION_ENTERPRISE_SEARCH_PORT: ${EXTENSION_ENTERPRISE_SEARCH_PORT}
      CONTAINER_EXTENSION_FILEBEAT_NAME: ${CONTAINER_EXTENSION_FILEBEAT_NAME}
      CONTAINER_EXTENSION_FLEET_NAME: ${CONTAINER_EXTENSION_FLEET_NAME}
      EXTENSION_FLEET_PORT: ${EXTENSION_FLEET_PORT}
      APM_SERVER_PORT: ${APM_SERVER_PORT}
      CONTAINER_EXTENSION_HEARTBEAT_NAME: ${CONTAINER_EXTENSION_HEARTBEAT_NAME}
      CONTAINER_EXTENSION_METRICBEAT_NAME: ${CONTAINER_EXTENSION_METRICBEAT_NAME}
    networks:
      - ${NETWORK_NAME}
    depends_on:
      - elasticsearch
    restart: unless-stopped

  kibana:
    build:
      context: kibana/
      args:
        ELASTIC_VERSION: ${ELASTIC_VERSION}
    container_name: ${CONTAINER_KIBANA_NAME}
    volumes:
      - ./kibana/config/kibana.yml:/usr/share/kibana/config/kibana.yml:ro,Z
    ports:
      - 5601:5601
    environment:
      KIBANA_SYSTEM_PASSWORD: ${KIBANA_SYSTEM_PASSWORD:-}
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD:-}
      METRICBEAT_INTERNAL_PASSWORD: ${METRICBEAT_INTERNAL_PASSWORD:-}
      FILEBEAT_INTERNAL_PASSWORD: ${FILEBEAT_INTERNAL_PASSWORD:-}
      HEARTBEAT_INTERNAL_PASSWORD: ${HEARTBEAT_INTERNAL_PASSWORD:-}
      MONITORING_INTERNAL_PASSWORD: ${MONITORING_INTERNAL_PASSWORD:-}
      BEATS_SYSTEM_PASSWORD: ${BEATS_SYSTEM_PASSWORD:-}
      ELASTIC_PORT: ${ELASTIC_PORT}
      NETWORK_NAME: ${NETWORK_NAME}
      CONTAINER_SETUP_NAME: ${CONTAINER_SETUP_NAME}
      CONTAINER_ELASTICSEARCH_NAME: ${CONTAINER_ELASTICSEARCH_NAME}
      CONTAINER_KIBANA_NAME: ${CONTAINER_KIBANA_NAME}
      KIBANA_PORT: ${KIBANA_PORT}
      CONTAINER_LOGSTASH_NAME: ${CONTAINER_LOGSTASH_NAME}
      LOGSTASH_PORT: ${LOGSTASH_PORT}
      CONTAINER_EXTENSION_CURATOR_NAME: ${CONTAINER_EXTENSION_CURATOR_NAME}
      CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME: ${CONTAINER_EXTENSION_ENTERPRISE_SEARCH_NAME}
      EXTENSION_ENTERPRISE_SEARCH_PORT: ${EXTENSION_ENTERPRISE_SEARCH_PORT}
      CONTAINER_EXTENSION_FILEBEAT_NAME: ${CONTAINER_EXTENSION_FILEBEAT_NAME}
      CONTAINER_EXTENSION_FLEET_NAME: ${CONTAINER_EXTENSION_FLEET_NAME}
      EXTENSION_FLEET_PORT: ${EXTENSION_FLEET_PORT}
      APM_SERVER_PORT: ${APM_SERVER_PORT}
      CONTAINER_EXTENSION_HEARTBEAT_NAME: ${CONTAINER_EXTENSION_HEARTBEAT_NAME}
      CONTAINER_EXTENSION_METRICBEAT_NAME: ${CONTAINER_EXTENSION_METRICBEAT_NAME}
    networks:
      - ${NETWORK_NAME}
    depends_on:
      - elasticsearch
    restart: unless-stopped

networks:
  afa-net:
    external: true
  dev-afa-net:
    external: true

volumes:
  elasticsearch:
